# 复制为 .env.development 或 .env.production

# true = 内存 Mock，无需后端；false = 请求真实 API（开发走 Vite 代理）
VITE_USE_MOCK=false

# 生产环境 API 根路径，如 https://api.example.com（末尾勿加斜杠）
# 开发留空即可，由 vite.config.ts 代理 /api 与 /uploads
VITE_API_BASE=
