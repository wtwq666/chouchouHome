# Vercel 环境变量（在 Vercel 控制台 → Settings → Environment Variables 配置）
# 不要用 Mock；必须指向已部署的 FastAPI 公网地址（无末尾斜杠）
# 有 api 子域名+HTTPS 后改为 https://api.你的域名.com

VITE_USE_MOCK=false
VITE_API_BASE=http://129.211.3.207:8000
